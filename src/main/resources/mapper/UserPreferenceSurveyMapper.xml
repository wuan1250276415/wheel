<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basebackend.wheel.mapper.UserPreferenceSurveyMapper">
    <resultMap id="BaseResultMap" type="com.basebackend.wheel.entity.UserPreferenceSurvey">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="selected_categories" jdbcType="VARCHAR" property="selectedCategories"
                typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
        <result column="skipped" jdbcType="BOOLEAN" property="skipped"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="create_by" jdbcType="BIGINT" property="createBy"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="update_by" jdbcType="BIGINT" property="updateBy"/>
        <result column="deleted" jdbcType="BOOLEAN" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, selected_categories, skipped, create_time, create_by, update_time, update_by, deleted
    </sql>

    <!-- 根据用户ID查询偏好调查 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM user_preference_survey
        WHERE user_id = #{userId,jdbcType=BIGINT}
        AND deleted = 0
    </select>

    <!-- 检查用户是否已完成调查 -->
    <select id="existsByUserId" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM user_preference_survey
            WHERE user_id = #{userId,jdbcType=BIGINT}
            AND deleted = 0
        )
    </select>
</mapper>
