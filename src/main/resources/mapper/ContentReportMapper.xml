<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basebackend.wheel.mapper.ContentReportMapper">
  <resultMap id="BaseResultMap" type="com.basebackend.wheel.entity.ContentReport">
    <!--@mbg.generated-->
    <!--@Table wheel_api.content_report-->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="content_id" jdbcType="BIGINT" property="contentId" />
    <result column="reporter_user_id" jdbcType="BIGINT" property="reporterUserId" />
    <result column="report_type" jdbcType="BOOLEAN" property="reportType" />
    <result column="report_time" jdbcType="TIMESTAMP" property="reportTime" />
    <result column="report_reason" jdbcType="BOOLEAN" property="reportReason" />
    <result column="report_description" jdbcType="LONGVARCHAR" property="reportDescription" />
    <result column="handle_status" jdbcType="BOOLEAN" property="handleStatus" />
    <result column="handler_id" jdbcType="BIGINT" property="handlerId" />
    <result column="handled_at" jdbcType="TIMESTAMP" property="handledAt" />
    <result column="handle_result" jdbcType="LONGVARCHAR" property="handleResult" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    <result column="create_by" jdbcType="INTEGER" property="createBy" />
    <result column="update_by" jdbcType="INTEGER" property="updateBy" />
    <result column="deleted" jdbcType="BOOLEAN" property="deleted" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    id, content_id, reporter_user_id, report_type, report_time, report_reason, report_description, 
    handle_status, handler_id, handled_at, handle_result, create_time, update_time, create_by, 
    update_by, deleted
  </sql>

    <!-- 根据内容ID查询举报记录 -->
    <select id="selectByContentId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM content_report
        WHERE content_id = #{contentId,jdbcType=BIGINT}
        ORDER BY report_time DESC
    </select>

    <!-- 根据举报人ID查询举报记录 -->
    <select id="selectByReportUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM content_report
        WHERE reporter_user_id = #{reportUserId,jdbcType=BIGINT}
        ORDER BY report_time DESC
    </select>

    <!-- 查询未处理的举报 -->
    <select id="selectUnhandledReports" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM content_report
        WHERE handle_status = 0
        ORDER BY report_time ASC
    </select>

    <!-- 统计举报类型分布 -->
    <select id="countReportTypes" resultType="java.util.Map">
        SELECT
        report_type as reportType,
        COUNT(*) as count
        FROM content_report
        WHERE report_time &gt;= DATE_SUB(NOW(), INTERVAL #{days,jdbcType=INTEGER} DAY)
        GROUP BY report_type
    </select>

    <!-- 更新处理状态 -->
    <update id="updateHandleStatus">
        UPDATE content_report
        SET handle_status = #{handleStatus,jdbcType=TINYINT},
        handler_id = #{handleUserId,jdbcType=BIGINT},
        handled_at = NOW(),
            handle_result = #{handleResult,jdbcType=VARCHAR},
        update_time = NOW(),
            update_by = #{handleUserId,jdbcType=BIGINT}
        WHERE reporter_user_id = #{reportId,jdbcType=BIGINT}
    </update>

    <!-- 批量更新处理状态 -->
    <update id="batchUpdateHandleStatus">
        UPDATE content_report
        SET handle_status = #{handleStatus,jdbcType=TINYINT},
        handler_id = #{handleUserId,jdbcType=BIGINT},
        handled_at = NOW(),
            handle_result = #{handleResult,jdbcType=VARCHAR},
            update_time = NOW(),
            update_by = #{handleUserId,jdbcType=BIGINT}
        WHERE reporter_user_id IN
        <foreach close=")" collection="reportIds" item="id" open="(" separator=",">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>

    <!-- 查询重复举报 -->
    <select id="selectDuplicateReports" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM content_report
        WHERE content_id = #{contentId,jdbcType=BIGINT}
        AND handle_status = 0
        AND report_time &gt;= DATE_SUB(NOW(), INTERVAL #{days,jdbcType=INTEGER} DAY)
    </select>
</mapper>