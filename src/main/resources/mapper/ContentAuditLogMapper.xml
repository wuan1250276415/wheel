<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basebackend.wheel.mapper.ContentAuditLogMapper">

    <resultMap id="BaseResultMap" type="com.basebackend.wheel.entity.ContentAuditLog">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="content_id" property="contentId" jdbcType="BIGINT"/>
        <result column="submit_user_id" property="submitUserId" jdbcType="BIGINT"/>
        <result column="audit_type" property="auditType" jdbcType="TINYINT"/>
        <result column="audit_status" property="auditStatus" jdbcType="TINYINT"/>
        <result column="audit_comment" property="auditComment" jdbcType="VARCHAR"/>
        <result column="ai_audit_result" property="aiAuditResult" jdbcType="VARCHAR"/>
        <result column="audited_at" property="auditedAt" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="BIGINT"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="update_by" property="updateBy" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, content_id, submit_user_id, audit_type, audit_status, audit_comment,
        ai_audit_result, audited_at, create_time, create_by, update_time, update_by
    </sql>

    <!-- 根据内容ID查询审核日志 -->
    <select id="selectByContentId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM content_audit_log
        WHERE content_id = #{contentId,jdbcType=BIGINT}
        ORDER BY audited_at DESC
    </select>

    <!-- 根据审核人ID查询审核日志 -->
    <select id="selectByAuditUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM content_audit_log
        WHERE submit_user_id = #{auditUserId,jdbcType=BIGINT}
        ORDER BY audited_at DESC
    </select>

    <!-- 统计审核员工作量 -->
    <select id="countAuditWorkload" resultType="java.util.Map">
        SELECT
        submit_user_id as auditUserId,
        COUNT(*) as totalCount,
        SUM(CASE WHEN audit_status = 1 THEN 1 ELSE 0 END) as passCount,
        SUM(CASE WHEN audit_status = 2 THEN 1 ELSE 0 END) as rejectCount
        FROM content_audit_log
        WHERE audited_at >= DATE_SUB(NOW(), INTERVAL #{days,jdbcType=INTEGER} DAY)
        GROUP BY submit_user_id
    </select>

    <!-- 查询最近的审核记录 -->
    <select id="selectRecentAuditLogs" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM content_audit_log
        ORDER BY audited_at DESC
        LIMIT #{limit,jdbcType=INTEGER}
    </select>

    <!-- 批量插入审核日志 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO content_audit_log (
        content_id, submit_user_id, audit_type, audit_status, audit_comment,
        ai_audit_result, audited_at, create_time, create_by, update_time, update_by
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.contentId,jdbcType=BIGINT},
            #{item.auditUserId,jdbcType=BIGINT},
            #{item.auditType,jdbcType=TINYINT},
            #{item.auditStatus,jdbcType=TINYINT},
            #{item.auditReason,jdbcType=VARCHAR},
            #{item.aiAuditResult,jdbcType=VARCHAR},
            #{item.auditTime,jdbcType=TIMESTAMP},
            NOW(),
            #{item.auditUserId,jdbcType=BIGINT},
            NOW(),
            #{item.auditUserId,jdbcType=BIGINT}
            )
        </foreach>
    </insert>
</mapper>
