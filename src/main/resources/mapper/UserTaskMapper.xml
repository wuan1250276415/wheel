<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.basebackend.wheel.mapper.UserTaskMapper">

    <select id="selectByUserIdAndPeriod" resultType="com.basebackend.wheel.entity.UserTask">
        SELECT * FROM user_task
        WHERE user_id = #{userId}
        <if test="periodType != null">
            AND period_start = #{periodStart}
        </if>
        ORDER BY status ASC, id DESC
    </select>

    <update id="updateProgress">
        UPDATE user_task
        SET current_count = #{currentCount},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectExpiredTasks" resultType="com.basebackend.wheel.entity.UserTask">
        SELECT * FROM user_task
        WHERE status IN (0, 1)
        AND period_end &lt; #{currentDate}
    </select>

</mapper>
